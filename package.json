{
  "name": "pixel-canvas",
  "version": "0.4.14",
  "license": "MIT",
  "main": "./lib/index.js",
  "typings": "./lib/index.d.ts",
  "scripts": {
    "build": "rm -rf lib && mkdir -p lib && cp -r src lib && find lib -name \\*.ts -delete && npm run build:js",
    "build:js": "tsc --project . && ngi --compress lib",
    "fixlint": "tslint --project . --fix true",
    "lint": "tslint --project ."
  },
  "dependencies": {
    "@angular-redux/store": "^7.0.1",
    "@angular/animations": "^5.0.0",
    "@angular/cdk": "^5.0.0",
    "@angular/common": "^5.0.0",
    "@angular/core": "^5.0.0",
    "@angular/forms": "^5.0.0",
    "@angular/material": "^5.0.0",
    "@angular/platform-browser": "^5.0.0",
    "ngx-color-picker": "^5.0.5",
    "redux": "^3.7.2",
    "redux-undo": "^1.0.0-beta9-9-7",
    "rxjs": "^5.5.2"
  },
  "devDependencies": {
    "angular-inliner-cli": "^1.2.1",
    "tslint": "^5.8.0",
    "typescript": "~2.4.2"
  }
}
